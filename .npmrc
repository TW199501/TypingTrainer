# Refuse to install on an unsupported Node rather than warning and failing later.
#
# package.json asks for >=24.18 because scripts/export-schema.mjs uses node:sqlite,
# which needs --experimental-sqlite before Node 24. Without this line that is only
# an EBADENGINE warning during install, and the real failure arrives much later
# somewhere unrelated.
engine-strict=true
