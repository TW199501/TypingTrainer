node_modules
dist
coverage
package-lock.json
# The exported design prototype is kept verbatim.
docs/design/
# Build output of the desktop shell and the backend.
src-tauri/target
src-tauri/gen
server/**/bin
server/**/obj

# Linked git worktrees other tools create inside the repo. They hold a second
# copy of every file, including docs/design, whose ignore rule above is
# anchored to the root and so does not reach them.
.kilo/
